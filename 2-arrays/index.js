let colors = ['groen', 'blauw', 'rood'];
let lastNum = [colors.length - 1];
let total = colors.push('geel', 5, {greeting:"Hi ik ben een object"});


console.log (colors);

console.log (colors.length);

console.log (colors [0]);


console.log (colors [lastNum]);

console.log (colors.slice(-1));
